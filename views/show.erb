<p>タイトル：<%= h(@hash["title"]) %></p>

<% text = h(@hash["body"]) %>
<%= text.gsub(/\n/, "<br>") %>

<p></p>

<form method="post" action="/show/<%= h(@hash["id"]) %>" >
<input type="hidden" name="_method" value="DELETE">
<input type="submit" value="削除">
</form>

  <p></p>

  <button type="button">
  <a href='/edit/<%= @hash["id"] %>'>編集</a>
  </button>  
  <p></p>
  <button type="button">
  <a href='/'>一覧に戻る</a>
  </button>
